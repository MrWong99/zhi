# Sample configuration for the workspace.
# The UI plugin provides an interface to manage these values.

app:
  name:
    val: "my-app"
    metadata:
      description: "Application name"
  version:
    val: "1.0.0"
    metadata:
      description: "Application version"
