# Sample configuration for the workspace.
# The store plugin persists these values.

app:
  name:
    val: "my-app"
    metadata:
      description: "Application name"
  version:
    val: "1.0.0"
    metadata:
      description: "Application version"
