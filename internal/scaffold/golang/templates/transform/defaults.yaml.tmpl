# Sample configuration for the workspace.
# The transform plugin modifies these values before display.

app:
  name:
    val: "my-app"
    metadata:
      description: "Application name"
  version:
    val: "1.0.0"
    metadata:
      description: "Application version"
