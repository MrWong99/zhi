all:
  hosts:
    web1:
      ansible_host: "10.0.1.10"
      ansible_user: deploy
      ansible_port: 22
    db1:
      ansible_host: "10.0.1.20"
      ansible_user: deploy
  children:
    webservers:
      hosts:
        web1:
      vars:
        http_port: "8080"
    databases:
      hosts:
        db1:
      vars:
        backup_schedule: "0 2 * * *"
